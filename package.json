{
  "name": "@evanoc3/website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "parcel serve src/**/*.html --port 8080 --cache-dir .cache --dist-dir build",
    "build": "parcel build src/**/*.html --cache-dir .cache --dist-dir build"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "sass": "^1.32.7",
    "typescript": "^4.1.5"
  }
}
